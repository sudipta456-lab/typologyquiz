# Google Admin's manual-install tag ID (shared tag); loads and configures gtag.js.
NEXT_PUBLIC_GA_TAG_ID=G-TZ9B8MB7SP
# Selected GA4 web-stream destination; used only for explicit event send_to.
NEXT_PUBLIC_GA_ID=G-RVHFFPF0W1
# Fail closed: enable only after account/destination settings and actual network QA.
# Both exact IDs and this flag are required at Next build time; rebuild to change.
# Shared-tag automatic-event fanout is not proven safe by explicit event routing.
NEXT_PUBLIC_GA_ENABLED=false
